import React from 'react';
import { View } from 'react-native';

const ViewPdf = ({ imageUrl, imageName, styles }) => {
    return (
        <View style={{ flex: 1 }}>
        </View>
    )
};
export default ViewPdf;